# Structured Network Layout

The Network uses a deterministic preset projection: watched media occupy the primary grid and selected relationship types occupy dedicated lanes. The backend graph contract is unchanged. This presentation avoids force-directed simulation and keeps filtering/focus as client-side projections.